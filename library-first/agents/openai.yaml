interface:
  display_name: "Library First"
  short_description: "Evaluate existing libraries before writing your own"
  default_prompt: "Use $library-first before hand-rolling a general mechanism: name the problem in domain vocabulary, search this language's own ecosystem and standard library, apply the quality gate, then state whether you adopted a package or wrote it yourself and why."
